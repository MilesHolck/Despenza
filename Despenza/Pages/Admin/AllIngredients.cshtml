@page
@model Despenza.Pages.Admin.AllIngredientsModel

<div class="inventory-page">
    <h1>Ingredients</h1>

    <div class="table-card">
        <table class="table table-bordered table-hover align-middle mb-0">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Navn</th>
                    <th>KiloPris</th>
                    <th>Unit</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var inventoryItem in Model.IngredientsList)
                {
                    <tr>
                        <td>@inventoryItem.Id</td>
                        <td>@inventoryItem.WareId</td>
                        @* <td>@inventoryItem.KiloPrice.ToString("N2")</td>
                        <td>@inventoryItem.Unit</td> *@
                        <td>
                            <a class="btn btn-dark btn-sm"
                                   asp-page="/Admin/EditIngredient"
                                   asp-route-id="@inventoryItem.Id">
                                Edit
                            </a>

                            <a class="btn btn-danger btn-sm"
                                   asp-page="/Admin/DeleteIngredient"
                                   asp-route-id="@inventoryItem.Id">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <a class="btn btn-dark mt-3"
           asp-page="/Admin/CreateIngredient">
        Create a new Ingredient
    </a>
</div>